# XDPGate 项目规则

本文件约定本仓库内的协作、文档和代码风格。除非用户明确要求，否则后续工作应
遵循这些规则。

## 默认语言

- 面向项目维护者的回复、文档、README、设计说明和故障排查说明默认使用简体中文。
- 代码标识符、命令、日志、协议字段、Linux/XDP/BPF/ODE 等专有名词可保留英文。
- 如果输出面向国际协作者或上游社区，可按需要使用英文。

## 代码注释

- 代码注释默认使用简体中文。
- 对协议布局、字节序、buffer 边界、socket 行为、ODE 参数、XDP verifier 约束、
  BPF map 语义、benchmark 指标等关键逻辑，应写清楚中文注释。
- 注释要解释“为什么这样做”和“有什么边界条件”，不要只重复代码表面含义。
- 公共接口、协议结构、容易误用的 helper function 应有中文说明。
- 复杂逻辑优先用小函数拆分，再配合中文注释。

## Windows 和云服务器环境

- Windows 上优先使用 PowerShell 命令。
- 涉及删除、重置、覆盖配置、卸载、批量改文件等操作时，应先说明风险，并优先给
  出更安全的检查或备份方案。
- 云服务器默认按 Ubuntu 22.04 LTS 处理。
- 中国出差网络环境下，依赖登录、下载、浏览器跳转、代理、软件源、GitHub 访问的
  操作都可能失败，应给出替代方案。

## 开发边界

- 第一阶段只做普通 UDP socket，不引入 XDP、AF_XDP、DPDK、复杂可靠 UDP 或完整游戏逻辑。
- XDP 只负责早期过滤和统计，不解析完整 cube payload，不包含 ODE/game logic。
- ODE simulation、用户态 UDP 网络、snapshot serialization、viewer、benchmark tools、
  XDP/eBPF 代码应分目录管理。

## 稳定性优先

- 优先选择保守、可验证、可逐步回退的方案。
- 先检查，再验证，再做最小修改，最后才考虑重装、重置或清理配置。
- 在腾讯云轻量服务器上，不用它证明百万 PPS 或 AF_XDP zero-copy 极限性能。

